# ===========================================
# Dev frontend + Production API
# ===========================================
# Copy to .env.development.local to use production API when running `pnpm dev`.
# Next.js loads .env.development.local in dev mode (overrides .env).
#
# NOTE: Prod API must allow CORS from http://localhost:3000 (CORS_ORIGIN).

NEXT_PUBLIC_API_URL=https://coinflipapi-production.up.railway.app
NEXT_PUBLIC_WS_URL=wss://coinflipapi-production.up.railway.app/ws
